To execute a script from this folder, call it like this:

    $ BYCEPS_CONFIG_FILE=config/config.toml ./create_some_stuff.py

Make sure that the virtual environment is active and that BYCEPS has
been installed in editable mode (see documentation on installation).
