server {
    listen 80;
    listen [::]:80;
    root /var/www/html;
    server_name ${SERVER_NAME_ADMIN};
    include includes/byceps;
}

server {
    listen 80;
    listen [::]:80;
    root /var/www/html;
    server_name ${SERVER_NAME_SITE};
    include includes/byceps;
}
